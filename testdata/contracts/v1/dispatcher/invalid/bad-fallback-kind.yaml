apiVersion: projectctl/v1
kind: DispatcherRepository
spec:
  ref: bad-fallback
  targets:
  - target:
      ref: alpha
      issueStore:
        owner:
          kind: organization
          login: example-org
        repository: shared-tasks
      project:
        owner:
          kind: organization
          login: example-org
        number: 1
    fields: {}
    dimensionBindings: {}
  - target:
      ref: beta
      issueStore:
        owner:
          kind: organization
          login: example-org
        repository: shared-tasks
      project:
        owner:
          kind: organization
          login: example-org
        number: 2
    fields: {}
    dimensionBindings: {}
  dimensions: {}
  routing:
    rules:
    - ref: alpha-route
      labelRef: route-1
      targetRef: alpha
    - ref: beta-route
      labelRef: route-2
      targetRef: beta
    fallback:
      kind: first
  sourceRefs: []
  requirements:
    features:
    - issues
    - projects-v2
    - issue-labels
    mutations: []
  labels:
    declarations:
      route-1:
        name: project:route-1
        color: 1f6feb
        description: Routes issues to target alpha.
        role:
          kind: project_route
          targetRef: alpha
      route-2:
        name: project:route-2
        color: 1f6feb
        description: Routes issues to target beta.
        role:
          kind: project_route
          targetRef: beta
    projectRouting:
      kind: required_label
